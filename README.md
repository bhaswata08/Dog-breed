# Dog-breed
Classify images based on dog breeds.
This model is trained on dog images with model architechture based on VGG, AlexNet and ResNet. Execute the scripts using the sh file. Upload your own images in the uploaded images folder. Small project. Have fun with it!
